// Non-web: no full page reload; app continues. User may restart manually.

/// Reloads the app from the server. On web this does a full page reload; on other platforms no-op.
void reloadApp() {
  // No-op on mobile/desktop. Cache is still cleared; user can restart the app manually.
}

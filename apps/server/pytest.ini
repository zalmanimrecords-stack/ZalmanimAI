[pytest]
testpaths = tests
# Keep session temps under the repo with a stable ignored folder (avoids stray pytest-cache-files-* / .pytest_tmp ACL issues on some Windows setups).
addopts = --basetemp=.pytest_basetemp
